# 小派作品发布节点使用说明

## 节点概述

**节点ID**: `xiaopai_publish`  
**节点名称**: 小派作品发布  
**分类**: publish  
**执行域**: process (独立子进程运行)  

这是一个专为小Pi助手设计的作品发布节点，可以自动准备发布数据并调用发布服务。

## 功能特性

1. **自动数据准备**: 从上游节点接收视频、封面、标题/描述等数据
2. **智能标题处理**: 支持标题前缀/后缀附加功能
3. **多平台发布**: 支持抖音、B站、小红书、YouTube等多平台
4. **定时发布**: 支持定时发布功能
5. **草稿模式**: 支持保存为平台草稿或本地草稿
6. **内容声明**: 支持AI生成内容、原创等声明

## 输入端口

| 端口ID | 标签 | 类型 | 必需 | 说明 |
|--------|------|------|------|------|
| `video` | 视频 | video | ✓ | 待发布的视频文件 |
| `cover_landscape` | 横屏封面 | image | | 横版封面图片（16:9） |
| `cover_portrait` | 竖屏封面 | image | | 竖版封面图片（9:16） |
| `json` | 标题/描述 | json | | 上游JSON数据（包含标题、描述、标签等） |

## 输出端口

| 端口ID | 标签 | 类型 | 说明 |
|--------|------|------|------|
| `text` | 发布结果 | text | 发布结果的JSON文本 |
| `result_file` | 结果文件 | file | 详细结果文件路径 |

## 配置字段

### 基础配置
- **选择发布账号**: 选择要发布到的平台账号（必填）
- **视频标题**: 发布时的标题（留空则从上游JSON读取）
- **视频描述**: 发布时的描述（留空则从上游JSON读取）
- **标签**: 逗号分隔的标签列表

### 标题/描述处理
- **标题附加文本**: 可为标题添加前缀或后缀
- **描述附加文本**: 可为描述添加前缀或后缀

### 发布选项
- **发布方式**: 
  - 直接发布
  - 存为平台草稿
  - 存为本地草稿
- **内容声明**: 选择内容属性声明
- **原创内容**: 是否声明为原创
- **定时发布**: 是否定时发布及发布时间

## 使用示例

### 基本工作流

```
视频下载 → 字幕翻译 → 小派作品发布
```

### 完整工作流

```
输入视频 → ASR识别 → 字幕生成 → 翻译 → 封面生成 → 小派作品发布
```

### 配置示例

```json
{
  "account_ids": ["account_1", "account_2"],
  "title": "我的视频标题",
  "title_affix": "【小Pi推荐】",
  "title_affix_mode": "prefix",
  "description": "视频描述内容",
  "tags": "AI,视频,小Pi",
  "publish_mode": "publish",
  "is_original": true,
  "declaration": "ai_generated",
  "schedule_enabled": false
}
```

## 执行流程

1. **验证输入**: 检查视频文件和账号配置
2. **检测视频方向**: 自动检测视频是横版还是竖版
3. **解析上游数据**: 从JSON输入读取标题、描述、标签
4. **处理文本**: 应用标题/描述的前缀/后缀
5. **准备封面**: 解析横版/竖版封面
6. **调用发布服务**: 根据配置调用发布API
7. **保存结果**: 生成发布结果JSON文件

## 注意事项

1. **账号有效性**: 确保选择的账号Cookie有效
2. **封面要求**: 
   - 横版封面: 16:9比例
   - 竖版封面: 9:16比例
3. **标题长度**: 注意各平台的标题长度限制
4. **定时发布**: 时间格式为 `yyyy-MM-dd HH:mm:ss`
5. **内容声明**: 根据平台要求选择合适的声明

## 错误处理

- **未选择账号**: 请在节点配置中选择发布账号
- **未找到视频**: 请连接视频输入端口
- **账号无效**: 请检查账号Cookie是否有效
- **发布失败**: 查看结果文件中的错误信息

## 产物说明

执行成功后会在任务目录的 `output/` 文件夹生成：
- `xiaopai_publish_result_{node_id}.json`: 包含详细的发布参数和结果

## 与其他节点的配合

### 与翻译节点配合
```
字幕翻译 → JSON转文本 → 小派作品发布
```

### 与封面生成节点配合
```
封面生成 → 小派作品发布（连接封面输出到封面输入）
```

### 与定时发布配合
配置定时发布时间，节点会在指定时间自动发布。

## 技术细节

- **执行域**: process（独立子进程运行，支持取消）
- **进度上报**: 0-100%进度实时更新
- **产物命名**: 包含节点ID后缀，支持多实例
- **错误处理**: 完整的错误捕获和报告机制

## 更新日志

- **v1.0**: 初始版本，支持基本发布功能
- 支持多平台账号选择
- 支持标题/描述前缀后缀
- 支持定时发布和草稿模式
- 支持内容声明和原创标识